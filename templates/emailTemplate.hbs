<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payroll for {{payrollData.week}}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }
    .container {
      width: 100%;
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      margin-bottom: 20px;
    }
    .header h1 {
      color: #4CAF50;
    }
    .table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    .table th, .table td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    .table th {
      background-color: #f2f2f2;
    }
    .footer {
      text-align: center;
      font-size: 12px;
      color: #888;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Payroll Report for Week {{payrollData.week}}</h1>
      <p>
        {{#if_eq payrollData.role 'driver'}}
          {{payrollData.driver.name}} - Driver Earnings
        {{/if_eq}}
        {{#if_eq payrollData.role 'dispatcher'}}
          {{payrollData.dispatcher.name}} - Dispatcher Earnings
        {{/if_eq}}
      </p>
        <p className="text-lg font-bold">{{payrollData.title}}</p>
        <p className="text-lg font-bold">Total Earnings: ${{payrollData.totalEarnings}}</p>
        <p className="text-sm text-gray-500">Number of Loads: {{payrollData.loads.length}}</p>
    </div>
    <table class="table">
      <thead>
        <tr>
          <th>From Location</th>
          <th>To Location</th>
          <th>Rate</th>
          {{#if_eq payrollData.role 'driver'}}
            <th>Driver Earnings</th>
          {{/if_eq}}
          {{#if_eq payrollData.role 'dispatcher'}}
            <th>Dispatcher Earnings</th>
          {{/if_eq}}
        </tr>
      </thead>
      <tbody>
        {{#each payrollData.loads}}
        <tr>
          <td>{{this.from_location}}</td>
          <td>{{this.to_location}}</td>
          <td>${{this.rate}}</td>
          {{#if_eq payrollData.role 'driver'}}
            <td>${{this.driverEarnings}}</td>
          {{/if_eq}}
          {{#if_eq payrollData.role 'dispatcher'}}
            <td>${{this.dispatcherEarnings}}</td>
          {{/if_eq}}
        </tr>
        {{/each}}
      </tbody>
    </table>
    <div class="footer">
      <p>This email was generated by OPER8.</p>
    </div>
  </div>
</body>
</html>
